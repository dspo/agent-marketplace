# Copilot compatibility configuration
# This file is source input for on-demand Copilot export.
# Run: python scripts/sync-skills.py --target copilot --output-dir <path> --skill playwright-cli
#
# NOTE: playwright-cli is a descriptive skill that wraps the external
# `playwright-cli` tool. No custom commands are defined here.
# Commands are not required for skills that only provide context and instructions.
