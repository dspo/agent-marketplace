{
  "name": "@mimo-ai/mimo-plugin-cc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Use MiMo from Claude Code to review code or delegate tasks.",
  "license": "MIT",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "build": "node build.mjs && tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  }
}
