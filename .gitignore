# Claude Code local artifacts
.claude/worktrees/

# Plugin build artifacts (compiled scripts are intentionally committed)
plugins/mimo/_build/node_modules/
plugins/mimo/_build/tests/.build/
plugins/remora/_build/node_modules/

# remora runtime state lives under ~/.pi/ (sessions: ~/.pi/agent/sessions/, config: ~/.pi/remora.config.yaml, blobs: ~/.pi/remora/blobs/).
# Legacy workspace .remora/ dirs may still exist locally (old flat-JSON sessions/config) — keep ignoring them.
.remora/

# OS files
.DS_Store
